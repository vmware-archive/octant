import { moduleMetadata } from '@storybook/angular';
import { Meta, Story, Props } from '@storybook/addon-docs/blocks';
import { object, withKnobs } from '@storybook/addon-knobs';
import {TimestampComponent} from "../app/modules/shared/components/presentation/timestamp/timestamp.component";

<h1>Timestamp component</h1>
<h2>Description</h2>

<p>
    Timestamp is used to present time.
    Time is presented in relative form based on current time.
    This is very useful when displaying age of objects or files.
</p>
<h2>Example</h2>

<Meta title="Octant|Components" component={TimestampComponent} />

<Story name="Timestamp" height="150px">
{{
  props: {
    view: object('View', {metadata: {type: "timestamp"}, config: {timestamp: 1588716648}}),
  },
  component: TimestampComponent,
}}
</Story>

<h2>Props</h2>
<Props of={TimestampComponent} />

