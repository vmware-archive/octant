import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { object, withKnobs } from '@storybook/addon-knobs';
import { ButtonComponent } from "../app/modules/shared/components/presentation/button/button.component";

export const buttonDocs= { source: { code: `button := component.NewButton()
component.NewButton("button",
      action.CreatePayload(octant.ActionDeleteObject, key.ToActionPayload()),
      component.WithButtonConfirmation(
        "Delete Pod",
        "Are you sure you want to delete *Pod* **pod**?",
      ))
`}}


<h1>Button component</h1>
<h2>Description</h2>

<p>A Button component is a Button that can exists without a Button Group component.</p>
<p>The Button component supports a confirmation method with a Markdown compatible message that appears in a modal.</p>

<h2>Example</h2>

<Meta title="Components/Button" component={ButtonComponent} />

<Canvas withToolbar>
  <Story name="Button component" parameters={{ docs: buttonDocs }} height="150px" >
  {{
      props: {
        view:  object('View', {
          config: {
            name: 'Delete',
            payload: {},
            confirmation: {
              title: 'Delete Pod',
              body: 'Are you sure you want to delete *Pod* **pod**?',
            },
          },
          metadata: {
            type: 'button',
          },
      }),
      },
      component: ButtonComponent,
      }}
  </Story>
</Canvas>

<h2>Props</h2>
<ArgsTable of={ButtonComponent} />
