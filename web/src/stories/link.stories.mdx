import { moduleMetadata } from '@storybook/angular';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { object, withKnobs } from '@storybook/addon-knobs';
import {LinkComponent} from "../app/modules/shared/components/presentation/link/link.component";

export const linkDocs= { source: { code: `title = "Example Link"
   ref = "https://example.org"
   value = "Example Link"
   component.NewLink(title, value, ref)`
}}

<h1>Link component</h1>
<h2>Description</h2>

<p>The Link component is used to present simple links</p>
<h2>Example</h2>

<Meta title="Components/Link" component={LinkComponent} />

<Preview withToolbar>
  <Story name="Link component" parameters={{ docs: linkDocs }} height="75px">
  {{
      props: {
        view:  object('View', {
          metadata: {
            type: 'link',
          },
          config: {
            value: 'Example Link',
            ref: 'https://example.org',
          },
        }),
      },
      component: LinkComponent,
      }}
  </Story>
</Preview>

<h2>Props</h2>
<Props of={LinkComponent} />
