import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { object, withKnobs } from '@storybook/addon-knobs';
import {TextComponent} from "../app/modules/shared/components/presentation/text/text.component";

export const textDocs= { source: { code: `text := component.NewText("sampleText")`}}

<h1>Text component</h1>
<h2>Description</h2>

<p>The Text component is used to present simple text</p>
<h2>Example</h2>

<Meta title="Components/Text" component={TextComponent} />

<Preview withToolbar>
  <Story name="Text component" parameters={{ docs: textDocs }} height="75px" >
  {{
      props: {
        view:  object('View', {
          metadata: {
            type: 'text',
          },
          config: {
            value: 'hello world',
            isMarkdown: false,
          },
        }),
      },
      component: TextComponent,
      }}
  </Story>
</Preview>

<h2>Props</h2>
<Props of={TextComponent} />
