import {ArgsTable, Canvas, Meta, Story} from '@storybook/addon-docs/blocks';
import {object} from '@storybook/addon-knobs';
import {SignpostComponent} from "../app/modules/shared/components/presentation/signpost/signpost.component";

export const signpostDocs = {
  source: {
    code: `c: component.NewSignpost(component.NewIcon("user"), "Message")
c.SetPosition = PositionTopMiddle
`
  }
}

<h1>Signpost Component</h1>
<h2>Description</h2>
<p>An Signpost component is used to display a message by clicking on a component.</p>

<p>On hover, the component will change color and cursor will change to a pointer.</p>

<h2>Example</h2>

<Meta title="Components/Signpost" component={SignpostComponent}/>

<Canvas withToolbar>
  <Story name="Signpost component" parameters={{docs: signpostDocs}} height="200px">
    {{
      props: {
        view: object('View', {
          "metadata": {
            "type": "signpost"
          },
          "config": {
            "trigger": {
              "metadata": {
                "type": "icon"
              },
              "config": {
                "shape": "user",
                "size": "",
                "direction": "",
                "solid": false,
                "inverse": false,
                "badge": "",
                "color": "",
                "badgeColor": "",
                "label": "",
              }
            },
            "message": "Message",
            "position": "right-middle"
          }
        }),
      },
      component: SignpostComponent,
    }}
  </Story>
</Canvas>

<h2>Props</h2>
<ArgsTable of={SignpostComponent}/>
